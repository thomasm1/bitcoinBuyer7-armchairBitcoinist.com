import { AppPasswordDirective } from './app-password.directive';

describe('AppPassword.Directive', () => {
  it('should create an instance', () => {
    const directive = new AppPasswordDirective(e);
    expect(directive).toBeTruthy();
  });
});
